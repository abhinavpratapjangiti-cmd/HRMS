<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>LovasIT HRMS - User Management</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" />

  <style>
    body { background: #f6f8fb; font-family: Inter, sans-serif; }
    .page-header { background: #fff; border-radius: 14px; padding: 18px 22px; box-shadow: 0 8px 24px rgba(15, 23, 42, 0.04); }
    .card { border: 0; border-radius: 16px; box-shadow: 0 10px 30px rgba(15, 23, 42, 0.05); }

    .employee-grid {
      display: grid;
      grid-template-columns: 2fr 1fr 1fr 1fr;
      align-items: center;
      gap: 12px;
    }

    .employee-row { background: #fff; border-radius: 12px; padding: 12px; margin-bottom: 10px; border: 1px solid #f1f5f9; transition: all 0.2s ease; }
    .employee-row:hover { box-shadow: 0 8px 20px rgba(15, 23, 42, 0.08); transform: translateY(-1px); }
    .employee-name { font-weight: 600; font-size: 15px; }
    .employee-email { font-size: 12px; color: #6b7280; }
    .stat-row { display: flex; justify-content: space-between; padding: 10px 0; border-bottom: 1px solid #f8fafc; font-size: 14px; }
    .dept-badge { font-size: 12px; background: #eef2ff; color: #4f46e5; padding: 2px 10px; border-radius: 8px; font-weight: 600; }

    .btn-action { padding: 5px 10px; font-size: 12px; border-radius: 8px; margin-right: 5px; }
    
    /* Small label styling for the form */
    .form-label.small { font-size: 0.85rem; font-weight: 500; }
  </style>
</head>

<body>

<div class="container-fluid p-4">
  <div class="page-header mb-4 d-flex justify-content-between align-items-center">
    <h4 class="mb-0">User Management</h4>
    <button class="btn btn-sm btn-outline-primary" onclick="window.refreshAll()"> <i class="fa fa-sync"></i> Refresh Data</button>
  </div>

  <div class="row g-4">
    <div class="col-xl-8">

      <div class="card mb-4">
        <div class="card-body p-4">
            <h5 class="card-title mb-4">Create Employee</h5>

            <h6 class="text-muted text-uppercase small fw-bold mb-3">Personal Information</h6>
            <div class="row g-3 mb-4">
                <div class="col-md-6">
                    <label class="form-label text-muted small">Full name</label>
                    <input type="text" id="name" class="form-control" placeholder="e.g. John Doe">
                </div>
                <div class="col-md-6">
                    <label class="form-label text-muted small">Work email</label>
                    <input type="email" id="email" class="form-control" placeholder="e.g. john@company.com">
                </div>
                <div class="col-md-12">
                    <label class="form-label text-muted small">Temporary password</label>
                    <input type="password" id="password" class="form-control" placeholder="Create a password">
                </div>
            </div>

            <h6 class="text-muted text-uppercase small fw-bold mb-3">Work Information</h6>
            <div class="row g-3 mb-4">
                <div class="col-md-6">
                    <label class="form-label text-muted small">Department</label>
                    <select id="department" class="form-select">
                        <option value="IT">IT</option>
                        <option value="Sales">Sales</option>
                        <option value="HR">HR</option>
                        <option value="Marketing">Marketing</option>
			<option value="Finance">Finance</option>
                        <option value="Operations">Admin And Operations</option>
                    </select>
                </div>
                <div class="col-md-6">
                    <label class="form-label text-muted small">Client / Project</label>
                    <input type="text" id="client" class="form-control" placeholder="e.g. Internal / Project X">
                </div>
                <div class="col-md-6">
                    <label class="form-label text-muted small">Work location</label>
                    <input type="text" id="location" class="form-control" placeholder="e.g. New York Office">
                </div>
                <div class="col-md-6">
                    <label class="form-label text-muted small">Role</label>
                    <select id="role" class="form-select">
                        <option value="employee">Employee</option>
                        <option value="manager">Manager</option>
                        <option value="hr">HR</option>
                        <option value="admin">Admin</option>
                    </select>
                </div>
            </div>

            <h6 class="text-muted text-uppercase small fw-bold mb-3">Reporting</h6>
            <div class="row g-3 align-items-end">
                <div class="col-md-8">
                    <label class="form-label text-muted small">Reporting manager</label>
                    <select id="createManager" class="form-select">
                        <option value="">No reporting manager</option>
                    </select>
                </div>
                <div class="col-md-4 text-end">
                    <button id="createBtn" class="btn btn-primary w-100" onclick="window.createUser()">
                        Create Employee
                    </button>
                </div>
            </div>
        </div>
      </div>
      <div class="card">
        <div class="card-header bg-transparent fw-bold border-bottom">Employee Directory</div>
        <div class="card-body">
          <div class="employee-header employee-grid fw-bold text-muted small mb-3 p-2 bg-light rounded">
            <div>Employee Info</div>
            <div>Role / Dept</div>
            <div>Manager</div>
            <div class="text-end">Actions</div>
          </div>
          <div id="employeesList">
            <div class="text-center py-3 text-muted">Loading directory...</div>
          </div>
        </div>
      </div>
    </div>

    <div class="col-xl-4">
      <div class="card mb-4">
        <div class="card-header bg-transparent fw-bold border-bottom">Organisation Snapshot</div>
        <div class="card-body" id="orgStats">
          <div class="text-center text-muted">Loading snapshot...</div>
        </div>
      </div>
      <div class="card">
        <div class="card-header bg-transparent fw-bold border-bottom">Departments Distribution</div>
        <div class="card-body" id="deptDistribution">
          <div class="text-center text-muted">Loading distribution...</div>
        </div>
      </div>
    </div>
  </div>
</div>

<div class="modal fade" id="editModal" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header"><h5>Edit Employee</h5><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div>
      <div class="modal-body">
        <input type="hidden" id="edit-id">

        <label class="small fw-bold">Full Name</label>
        <input id="edit-name" class="form-control mb-3">

        <label class="small fw-bold">Work Email</label>
        <input id="edit-email" class="form-control mb-3">

        <div class="row">
            <div class="col-6">
                <label class="small fw-bold">Role</label>
                <select id="edit-role" class="form-select mb-3">
                  <option value="employee">Employee</option>
                  <option value="manager">Manager</option>
                  <option value="hr">HR</option>
                  <option value="admin">Admin</option>
                </select>
            </div>
            <div class="col-6">
                <label class="small fw-bold">Department</label>
                <select id="edit-department" class="form-select mb-3">
                    <option value="IT">IT</option>
                    <option value="Sales">Sales</option>
                    <option value="HR">HR</option>
                    <option value="Marketing">Marketing</option>
                    <option value="Operations">Operations</option>
                </select>
		<div class="col-6">
		<label class="small fw-bold">Reporting Manager</label>
		<select id="edit-department" class="form-select mb-3">
		    <option
            </div>
        </div>
      </div>
      <div class="modal-footer">
        <button class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
        <button class="btn btn-primary" onclick="window.saveEdit()">Save Changes</button>
      </div>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script src="/assets/js/pages/manage-users.js"></script>

</body>
</html>
